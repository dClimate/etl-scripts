[project]
name = "etl-scripts"
# Semantic Versioning https://semver.org/
version = "0.1.0"
requires-python = ">=3.12"

# numcodecs, dask, pandas, numpy, and multiformats are imports in various files, but not in pyproject.toml dependencies, since they are required by xarray, so we just use the existing compatible versions that are installed when xarray is installed
dependencies = [
    # Needed for xarray to access google cloud storage service
    "gcsfs>=2024.12.0",
    "py-hamt",
    "xarray[io,parallel,viz]",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.uv.sources]
py-hamt = { git = "https://github.com/dClimate/py-hamt", rev = "76deec0aa553a3f7a3bb860910a6cd0686d0bdf1" }

[dependency-groups]
dev = ["ruff>=0.8.0"]
